public void processShape(Shape shape) {
    switch (shape.getType()) {
        case "Circle":
            // Process circle
            break;
        case "Square":
            // Process square
            break;
    }
}
